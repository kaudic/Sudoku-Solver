-- Deploy sudoku:constraint_actor to pg

BEGIN;

--ALTER TABLE actor
    --DROP CONSTRAINT actor_actor_surname_key,
    --DROP CONSTRAINT actor_actor_password_key,
    --DROP CONSTRAINT actor_actor_name_key;

COMMIT;
